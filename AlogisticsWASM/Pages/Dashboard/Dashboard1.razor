@page "/dashboard"

@using ALOG.Modelos.Modelos.Dtos
@using AlogisticsWASM.Layout
@using ALOG.Modelos.Modelos.Graficas
@using ALOG.Modelos.Modelos.Vacios.Peticiones
@using ALOGRepositorios.Services.Peticiones.IPeticiones
@using ALOG.Modelos.Modelos.FiltrosBusqueda
@using Newtonsoft.Json
@inject IReferenciaService referenciaService
@inject NavigationManager navigationManager
@inject IJSRuntime js

<div class="row mt-10 g-4">
    <div class="col-12 col-xxl-12">
        <div class="mb-8">
            <h2 class="mb-2">Dashboard Vacíos</h2>
            <h5 class="text-body-tertiary fw-semibold">En este apartado podrás visualizar el estatus de cada uno de los contenedores registrados en el sistema</h5>
        </div>
        <div class="row align-items-center g-4">
            <div class="col-12 col-md-auto">
                <div class="d-flex align-items-center">
                    <span class="fa-stack" style="min-height: 46px;min-width: 46px;"><span class="fa-solid fa-square fa-stack-2x dark__text-opacity-50 text-success-light" data-fa-transform="down-4 rotate--10 left-4"></span><span class="fa-solid fa-circle fa-stack-2x stack-circle text-stats-circle-success" data-fa-transform="up-4 right-3 grow-2"></span><span class="fa-stack-1x fa-solid fa-star text-success " data-fa-transform="shrink-2 up-8 right-6"></span></span>
                    <div class="ms-3">
                        <h4 class="mb-0">@EstadoCerrado Contenedores cerrados</h4>
                        <p class="text-body-secondary fs-9 mb-0">Cierre operativo</p>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-auto">
                <div class="d-flex align-items-center">
                    <span class="fa-stack" style="min-height: 46px;min-width: 46px;">
                        <span class="fa-solid fa-square fa-stack-2x dark__text-opacity-50 text-warning-light" data-fa-transform="down-4 rotate--10 left-4"></span>
                        <span class="fa-solid fa-circle fa-stack-2x stack-circle text-stats-circle-warning" data-fa-transform="up-4 right-3 grow-2"></span>
                        <span class="fa-stack-1x fa-solid fa-pause text-warning " data-fa-transform="shrink-2 up-8 right-6"></span></span>
                    <div class="ms-3">
                        <h4 class="mb-0">@EstadoAbierto Contenedores en proceso</h4>
                        <p class="text-body-secondary fs-9 mb-0">En proceso</p>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-auto">
                <div class="d-flex align-items-center">
                    <span class="fa-stack" style="min-height: 46px;min-width: 46px;"><span class="fa-solid fa-square fa-stack-2x dark__text-opacity-50 text-danger-light" data-fa-transform="down-4 rotate--10 left-4"></span><span class="fa-solid fa-circle fa-stack-2x stack-circle text-stats-circle-danger" data-fa-transform="up-4 right-3 grow-2"></span><span class="fa-stack-1x fa-solid fa-xmark text-danger " data-fa-transform="shrink-2 up-8 right-6"></span></span>
                    <div class="ms-3">
                        <h4 class="mb-0">@EstadoCancelado Contenedores cancelados o rechazados</h4>
                        <p class="text-body-secondary fs-9 mb-0">Cancelados</p>
                    </div>
                </div>
            </div>
        </div>
        <hr class="bg-body-secondary mb-6 mt-4" />
       @*  <div class="row flex-between-center mb-4 g-3">
            <div class="col-auto">
                <h3>Total de contenedores registrados</h3>
                <p class="text-body-tertiary lh-sm mb-0"></p>
            </div>
            <div class="col-8 col-sm-4">
                <button type="button" class="btn btn-primary px-5 px-sm-15" data-bs-dismiss="modal" @onclick="IrA">Ver detalles</button>
                 <select class="form-select form-select-sm" id="select-gross-revenue-month">
                    <option>Mar 1 - 31, 2022</option>
                    <option>April 1 - 30, 2022</option>
                    <option>May 1 - 31, 2022</option>
                </select>
            </div>
        </div> *@
        
        @* <div class="echart-total-sales-chart" style="min-height:320px;width:100%">
            <Alogistic.Client.Layout.Graficas.EChartComponent Data="@dataPoints"></Alogistic.Client.Layout.Graficas.EChartComponent>
        </div> *@
        <div class="mx-n4 px-4 mx-lg-n6 px-lg-6 bg-body-emphasis pt-7 pb-3 border-y">
            <div class="row flex-between-center mb-4 g-3">
                <div class="col-auto">
                    <h3>Total de contenedores registrados</h3>
                    <p class="text-body-tertiary lh-sm mb-0"></p>
                </div>
                <div class="col-8 col-sm-4">
                    <button type="button" class="btn btn-primary px-5 px-sm-15" data-bs-dismiss="modal" @onclick="IrA">Ver detalles</button>
                    @* <select class="form-select form-select-sm" id="select-gross-revenue-month">
                    <option>Mar 1 - 31, 2022</option>
                    <option>April 1 - 30, 2022</option>
                    <option>May 1 - 31, 2022</option>
                    </select> *@
                </div>
            </div>
            <div class="row">
                <div class="col-12 col-xl-7 col-xxl-6">
                    <BarraGrafica dataPoints="@dataPoints"></BarraGrafica>
                   
                </div>
            </div>
        </div>
        <div class="row">
           
            <div class="col-12 mt-10">
                <Alogistic.Client.Layout.Graficas.EChartComponent Data="@dataPoints"></Alogistic.Client.Layout.Graficas.EChartComponent>
                <Alogistic.Client.Layout.Graficas.Grafica1></Alogistic.Client.Layout.Graficas.Grafica1>
                @* <div class="echart-total-sales-chart" style="min-height:320px;width:100%">
                    <BarraGrafica dataPoints="@dataPoints"></BarraGrafica>
                </div> *@
            </div>
        </div>
        
    </div>
</div>

@code {

    public List<DataPoint> dataPoints { get; set; } = new List<DataPoint>();
    public Dictionary<string, int> datos = new Dictionary<string, int>();
    public Dictionary<string, int> datosContenedores = new Dictionary<string, int>();
    public List<PeticionesServicio> serivicos = new List<PeticionesServicio>();
    public List<PeticionesContenedor> contenedores = new List<PeticionesContenedor>();
    public FiltroReferencia filtro = new FiltroReferencia();

    

    private int EstadoAbierto = 0;
    private int EstadoCerrado = 0;
    private int EstadoCancelado = 0;

    //protected override async Task OnInitializedAsync() {

        
        // // await modulo.InvokeVoidAsync("initializeEChart");

        // filtro.IdAduana = 1;
        // filtro.IdEmpresa = 1;
        // filtro.IdLNegocio = 1;


        // ICollection<RespObtenerReferenciasDTO> referencias = await referenciaService.GetReferencias(filtro);

        // if (referencias.Count() > 0) {

        //     foreach (var referencia in referencias) {
        //         foreach (var contenedor in referencia.Contenedores) {
        //             contenedores.Add(contenedor);
        //             foreach (var servicio in contenedor.Servicios) {
        //                 serivicos.Add(servicio);
        //             }
        //         }
        //     }
        //     int c = 0;
        //     foreach (var serv in serivicos) {
        //         if (datos.ContainsKey(serv.DescServicio)) {
        //             c++;
        //             datos[serv.DescServicio] = c;
        //         } else {
        //             c = 1;
        //             datos.Add(serv.DescServicio, c);
        //         }
        //     }
        //     c = 0;
        //     foreach (var cont in contenedores) {
        //         if (datosContenedores.ContainsKey(cont.EstadoContenedor)) {
        //             c++;
        //             datosContenedores[cont.EstadoContenedor] = c;
        //         } else {
        //             c = 1;
        //             datosContenedores.Add(cont.EstadoContenedor, c);
        //         }
        //     }

        //     datosContenedores.Add("CERRADO", 0);
        //     datosContenedores.Add("CANCELADO", 0);

        //     EstadoAbierto = int.Parse(datosContenedores["ABIERTO"].ToString());
        //     EstadoCerrado = int.Parse(datosContenedores["CERRADO"].ToString());
        //     EstadoCancelado = int.Parse(datosContenedores["CANCELADO"].ToString());

        //     foreach (var d in datos) {
        //         dataPoints.Add(new DataPoint { Name = d.Key, Value = d.Value });
        //     }

        // }

   // }

    private void IrA() {
        navigationManager.NavigateTo("/referencias");    
    }

}