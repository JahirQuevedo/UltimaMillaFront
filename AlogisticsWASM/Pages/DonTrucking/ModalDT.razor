@* <div class="modal fade" id="modalContenedor" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="staticBackdropLabel">@TituloFormulario</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <EditForm Model="Contenedor" OnValidSubmit="@( () => CrearContenedor())">
                    <DataAnnotationsValidator></DataAnnotationsValidator>
                    <ValidationSummary></ValidationSummary>
                    <div class="row">
                        <div class="col-xl-12">
                            <form class="row g-3 mb-6">
                                <div class="col-sm-12 col-md-4">
                                    <div class="form-floating">
                                        <InputText class="form-control" id="floatingInputGrid" type="text" placeholder="Número de contenedor" @bind-Value="@Contenedor.NumeroContenedor" />
                                        <label for="floatingInputGrid">Número de contenedor</label>
                                    </div>
                                </div>
                                <div class="col-sm-12 col-md-4">
                                    <div class="form-floating">
                                        <InputText class="form-control" id="floatingInputGrid" type="text" placeholder="Referencia del cliente" @bind-Value="@Contenedor.RefenciaCliente" />
                                        <label for="floatingInputGrid">Referencia del cliente</label>
                                        <label for="floatingSelectPrivacy">Referencia del cliente</label>
                                    </div>
                                </div>
                                <ListadoTiposContenedor OnTipoContenedorSeleccionado="ObtenerTipoContenedorSeleccionado"></ListadoTiposContenedor>
                                <div class="col-sm-12 col-md-12">
                                    <ListadoGenerico Etiqueta="Clientes"
                                                     TItem="Cliente"
                                                     Listado="clientes"
                                                     IdSelector="c => c.IdCatCliente"
                                                     DisplaySelector="c => c.RazonSocial"
                                                     SelectedId="Contenedor.ClienteId"
                                                     OnSelected="ObtenerClienteSeleccionado" />
                                </div>
                                <div class="col-sm-12 col-md-12">
                                    <ListadoGenerico Etiqueta="Navieras"
                                                     TItem="Naviera"
                                                     Listado="navieras"
                                                     IdSelector="n => n.IdCatNaviera"
                                                     DisplaySelector="n => n.RazonSocial"
                                                     SelectedId="Contenedor.Naviera_Id"
                                                     OnSelected="ObtenerNavieraSeleccionada" />
                                </div>
                                <div class="col-sm-12 col-md-12">
                                    <ListadoGenerico Etiqueta="Patios"
                                                     TItem="Patio"
                                                     Listado="patios"
                                                     IdSelector="p => p.IdCatPatios"
                                                     DisplaySelector="p => p.RazonSocial"
                                                     SelectedId="Contenedor.PatioId"
                                                     OnSelected="ObtenerPatioSeleccionado" />
                                </div>
                                <div class="col-sm-12 col-md-4">
                                    <div class="form-floating">
                                        <InputText class="form-control" id="floatingInputGrid" type="text" placeholder="BL" @bind-Value="@Contenedor.Bl" />
                                        <label for="floatingInputGrid">Bl</label>
                                        <label for="floatingSelectPrivacy">Bl</label>
                                    </div>
                                </div>
                                <div class="col-sm-12 col-md-4">
                                    <div class="form-floating">
                                        <InputText class="form-control" id="floatingInputGrid" type="text" placeholder="Buque" @bind-Value="@Contenedor.Buque" />
                                        <label for="floatingInputGrid">Buque</label>
                                        <label for="floatingSelectPrivacy">Buque</label>
                                    </div>
                                </div>
                                <div class="col-sm-12 col-md-4">
                                    <div class="form-floating">
                                        <InputText class="form-control" id="floatingInputGrid" type="text" placeholder="Buque" @bind-Value="@Contenedor.Buque" />
                                        <label for="floatingInputGrid">Buque</label>
                                        <label for="floatingSelectPrivacy">Buque</label>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </EditForm>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-phoenix-primary px-5" data-bs-dismiss="modal" @onclick="CancelarAccion">Cancelar</button>
                <button type="button" class="btn btn-primary px-5 px-sm-15" data-bs-dismiss="modal" @onclick="@( () => CrearContenedor())">@TextBoton</button>
            </div>
        </div>
    </div>
</div> *@